//jQuery(function($){
   // $("main").on('click', function (e){ // событие клика по странице
       // if (!$("#menu").is(e.target) && // если клик сделан не по элементу
        //    $("#menu").has(e.target).length === 0) { // если клик сделан не по вложенным элементам
        //    $("#menu").fadeOut(); // скрываем блок
       // }
   //});
//});